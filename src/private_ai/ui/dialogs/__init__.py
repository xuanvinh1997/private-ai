"""Modal and modeless dialogs.

Imported lazily by their callers rather than re-exported here: a dialog that fails to
import must not take the view that never opens it down with it.
"""

from __future__ import annotations
